# ──────────────────────────────────────────────────────────────
# Vercel-only requirements (slim).
# Excludes pandas, numpy, boto3, twilio, google-api, psycopg2
# which are NOT needed for the demo and bloat the bundle by ~210 MB.
# ──────────────────────────────────────────────────────────────

# Core
Flask==3.0.3
Werkzeug==3.0.3
SQLAlchemy==2.0.31
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
alembic==1.13.2

# Config
python-dotenv==1.0.1

# Auth & Security
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.2
bcrypt==4.1.3
Flask-Bcrypt==1.0.1
pyotp==2.9.0
qrcode==7.4.2

# Files / Images
Pillow==10.4.0

# Reports — openpyxl for Excel export (lightweight),
# reportlab for PDF export (loaded by reports route)
openpyxl==3.1.5
reportlab==4.2.2

# Communication — email only, twilio excluded for demo
Flask-Mail==0.10.0

# Scheduling — imported by app but patched to no-op in api/index.py
APScheduler==3.10.4

# Validator
email_validator
